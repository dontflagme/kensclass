accepts a file with a set of lines KWIC
future (but not required now): use of a database instead of a file, a GUI front end instead of a console Possible
line can be "circularly" shifted by removing the first word in the line and appending it to the end of the line Each
line has a set of words, each word consists of a set of characters Each
outputs a listing (console + file) of all circular shifts of all lines in alphabetical order KWIC
